[project]
name = "pyperformance_bm_pickle"
requires-python = ">=3.8"
dependencies = ["pyperf@git+https://github.com/Yhg1s/pyperf@local-changes#egg=pyperf"]
urls = {repository = "https://github.com/python/pyperformance"}
dynamic = ["version"]

[tool.pyperformance]
name = "pickle"
tags = "serialize"
extra_opts = ["pickle"]
