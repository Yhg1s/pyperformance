[project]
name = "pyperformance_bm_genshi"
requires-python = ">=3.8"
dependencies = [
    "pyperf@git+https://github.com/Yhg1s/pyperf@local-changes#egg=pyperf",
    "Genshi",
]
urls = {repository = "https://github.com/python/pyperformance"}
dynamic = ["version"]

[tool.pyperformance]
name = "genshi"
tags = "template"
